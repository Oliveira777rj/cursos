<h1>Livros disponíveis:</h1>
<div class="books-container">
  {{#each books}}
  <div>
    #{{this.id}} - <span class="bold"><a href="/books/{{this.id}}">{{this.title}}</a></span> - {{this.pages}} páginas - <span class="bols"> Autor(a): {{this.autor}}</span>
  </div>
  {{/each}}
</div>